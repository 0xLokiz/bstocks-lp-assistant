# riskscreen.py itself uses only the Python standard library.
# This file is for running the test suite and CI's lint/type-check/dependency-scan jobs:
pytest>=7.0
ruff>=0.16
mypy>=1.10
pip-audit>=2.7
